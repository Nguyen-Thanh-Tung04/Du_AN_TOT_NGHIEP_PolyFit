(function ($) {
    "use strict";
    var HT = {};
    // var _token = $('meta[name="csrf-token"]').attr('content');

    HT.calculateTotal = () => {
    };

    $(document).ready(function () {
         
    });

})(jQuery);
